# gpu-queuer
This project is a scheduling and pre-scoring for other projects that are supposed to run on GPU server. As a multi-attribute decision scheme the current algorithm scores each other projects by several criteria and shows them in a near real-time up-to-date table. One can sort by name, project name, deadline or TOPSIS score. 
